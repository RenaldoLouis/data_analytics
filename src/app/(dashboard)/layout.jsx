import ClientLayoutShellDashboard from "./ClientLayoutShellDashboard";

export default function DashboardLayout({ children }) {
    return (
        <ClientLayoutShellDashboard>
            {children}
        </ClientLayoutShellDashboard>
    );
}
